# Core dependencies
python-telegram-bot==20.7
firebase-admin==6.5.0
python-dotenv==1.0.0

# Utilities (optional, only if bot uses images/audio/QR)
Pillow==10.1.0
qrcode==7.4.2
pydub==0.25.1

# Async & HTTP
aiohttp==3.9.1

# Web Dashboard (only if dashboard is used)
fastapi==0.104.1
uvicorn==0.24.0
jinja2==3.1.2

# Security
cryptography==41.0.7
bcrypt==4.1.2
